# Notebooks

Current notebooks are historical references and live under `notebooks/legacy/`.
They are not required for normal analysis, health checks, evaluation, or
training planning.

If notebook logic is needed for active development, move the reusable code into
`src/` and expose it through a CLI in `scripts/` before relying on it.
